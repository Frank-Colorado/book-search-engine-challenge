import { gql } from "@apollo/client";

// This is mutation called LOGIN_USER that returns an Auth type.
export const LOGIN_USER = gql`
  mutation login($email: String!, $password: String!) {
    login(email: $email, password: $password) {
      token
      user {
        _id
        username
        email
      }
    }
  }
`;
